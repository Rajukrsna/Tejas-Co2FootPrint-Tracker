<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Tejas AI</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- Font Awesome -->
    <style>
        /* General Body Styling */
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #f0f8ff; /* Light blue background */
            margin: 0;
            padding: 0;
            color: #2d6a4f; /* Dark green for text */
        }

        /* Main Content Styling */
        .container {
            margin-top: 70px;
            padding: 30px;
            background-color: #ffffff;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1); /* Soft shadow */
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
            text-align: center;
        }

        /* Header Styling */
        h1 {
            font-size: 36px;
            color: #40916c; /* Dark green */
            margin-bottom: 20px;
        }

        /* Form Styling */
        .form-control {
            padding-left: 3rem; /* Adds space for the icons */
            border: 2px solid #74c69d; /* Subtle green border */
            border-radius: 8px;
            height: 45px;
        }

        .form-control:focus {
            outline: none;
            border-color: #52b788; /* Darker green on focus */
            box-shadow: 0 0 8px rgba(82, 183, 136, 0.5);
        }

        .form-label {
            font-weight: bold;
            color: #2d6a4f;
        }

        /* Button Styling */
        .btn-primary {
            background-color: #40916c;
            border: none;
            padding: 12px 20px;
            border-radius: 8px;
            font-size: 18px;
            width: 100%;
            margin-top: 20px;
        }

        .btn-primary:hover {
            background-color: #52b788; /* Slightly lighter green on hover */
        }

        /* AWS Cognito Section */
        .aws-logo {
            margin-top: 20px;
            margin-bottom: 30px;
        }

        .aws-logo img {
            width: 150px;
        }

        .aws-cognito-text {
            font-size: 18px;
            color: #40916c;
            font-weight: bold;
            margin-bottom: 15px;
        }

        /* Footer Styling */
        
        a {
            color: #40916c;
            text-decoration: none;
        }

        a:hover {
            color: #52b788;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <!-- Main Content -->
    <div class="container">
        <!-- AWS Cognito Logo and Text -->
        <div class="aws-logo">
            <img src="/public/Cog.png" alt="AWS Cognito Logo">
        </div>

        <h1>Welcome to Tejas AI</h1>
        <p class="aws-cognito-text">Sign in using AWS Cognito for secure and seamless authentication.</p>

        <!-- Login Form -->
       
           
            <a href="/login" type="submit" class="btn btn-primary">Login</a>


       
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
