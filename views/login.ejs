<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"> <!-- Font Awesome -->
    <style>
        /* General Body Styling */
        body {
            font-family: 'Roboto', sans-serif;

            background-color: #f4f9f4; /* Soft natural green background */
            margin: 0;
            padding: 0;
            color: #2d6a4f; /* Dark green for text */
        }

        /* Main Content Styling */
        .container {
            margin-top: 70px;
            padding: 20px;
            background-color: white;
            border-radius: 12px;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15); /* Softer shadow */
            max-width: 450px;
            margin-left: auto;
            margin-right: auto;
        }

        .form-control {
            padding-left: 2.5rem; /* Adds space for the icons */
            border: 2px solid #74c69d; /* Subtle green border */
            border-radius: 8px;
        }

        .form-control:focus {
            outline: none;
            border-color: #52b788; /* Darker green on focus */
            box-shadow: 0 0 8px rgba(82, 183, 136, 0.5);
        }

        .btn-primary {
            background-color: #40916c; /* Dark green */
            border: none;
            padding: 10px;
            border-radius: 8px;
            font-size: 16px;
        }

        .btn-primary:hover {
            background-color: #52b788; /* Slightly lighter green on hover */
        }

        .form-label {
            font-weight: bold;
            color: #2d6a4f;
        }

       

        .form-group {
            position: relative;
        }

        /* Link Styling */
        a {
            color: #52b788;
            text-decoration: none;
        }

        a:hover {
            color: #40916c;
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <!-- Main Content -->
    <div class="container">
        <h1 class="text-center mb-4" style="color: #40916c;">Login</h1>
        <form action="/user/login" method="POST">
            <div class="mb-3 form-group">
                <label for="email" class="form-label">Email</label>
                <div class="input-group">
                   
                    <input type="email" class="form-control ps-5" id="email" name="email" required placeholder="Enter your email">
                </div>
            </div>
            <div class="mb-3 form-group">
                <label for="password" class="form-label">Password</label>
                <div class="input-group">
                    
                    <input type="password" class="form-control ps-5" id="password" name="password" required placeholder="Enter your password">
                </div>
            </div>
            <button type="submit" class="btn btn-primary w-100">Login</button>
        </form>
        <p class="text-center mt-3">Don't have an account? <a href="/user/register">Register here</a></p>
    </div>
    <a href="/user/auth/google">
        <button>
          <img src="/images/google-icon.png" alt="Google Icon" />
          Sign in with Google
        </button>
      </a>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
